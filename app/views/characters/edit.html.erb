<% title "Editing Character" %>

<%= form_for(@character) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


<%= link_to 'Back', @character %>
